import React from 'react';

const CompanyForm = () => {
    return (
        <div>
            {/* Nội dung của form thêm công ty */}
        </div>
    );
};

export default CompanyForm; // Đảm bảo rằng bạn đã export component này 